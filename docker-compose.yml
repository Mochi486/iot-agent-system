version: "3.9"

services:
  iot-agent-api:
    build: .
    container_name: iot-agent-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./iot_agent.db:/app/iot_agent.db
    restart: unless-stopped
